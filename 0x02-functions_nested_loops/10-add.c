#include "holberton.h"
#include <unistd.h>

/**
 * add - adds two integers
 *@n: holds on integer
 *@i: holds the other iteger to add to
 *
 * Return: the result
 */

int add(int n, int i)
{
	return (n + i);
}
