#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	sizeOf(char);
	sizeOf(int);
	sizeOf(long int);
	sizeOf(long long int);
	sizeOf(float);
	return (0);
}

